## 🐍 Steps to run the program
Open windows powershell/CMD by right clicking inside the project folder

command: `python --version`

command to run the program: `python main.py`

command to check files: `dir`

it will show project files and folders.
